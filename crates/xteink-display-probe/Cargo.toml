[package]
name = "xteink-display-probe"
version = "0.1.0"
edition = "2024"

[features]
default = []
defmt = ["dep:defmt", "embedded-hal-async/defmt-03"]

[dependencies]
defmt = { workspace = true, optional = true }
embedded-hal-async = { workspace = true }