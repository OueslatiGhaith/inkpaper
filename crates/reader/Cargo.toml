[package]
name = "inkpaper-reader"
version = "0.1.0"
edition = "2024"

[features]
default = []

[dependencies]
inkpaper-epub = { path = "../epub" }

[dev-dependencies]
futures-lite = { workspace = true }